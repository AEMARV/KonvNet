KonvNet is a framework using KL-Divergence as the linear layer.
The models are inspired by bayes theorem. The framework is deterministic but is a good backbone architecture for probabilistic models.

